const offerte = document.querySelectorAll("[data-prodotti]");
console.log(`le offerte`, offerte);

// offerte.forEach((offerta) => {
//   const prodCollegati = offerta.dataset.prodotti.split(/;/);
//   if (!prodCollegati.includes("2529")) offerta.classList.add("nascondi");
// });
